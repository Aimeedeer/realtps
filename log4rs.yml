appenders:
  console:
    kind: console

  file:
    kind: file
    path: jobs.log
    encoder:
      pattern: "{d} {l} {t} - {m}{n}"

root:
  level: info
  appenders:
    - console

loggers:
  realtps_import:
    level: info
    appenders:
      - file
    additive: true